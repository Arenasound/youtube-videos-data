name: Auto Update YouTube Videos

on:
  schedule:
    - cron: "0 6,18 * * *"  # Exécute à 06:00 et 18:00 UTC (2x par jour)
  workflow_dispatch:  # Permet de lancer l'action manuellement aussi

jobs:
  update-json:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      - name: Install dependencies
        run: npm install node-fetch@2

      - name: Run update script
        run: node updateVideos.js

      - name: Commit and Push changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add videos.json
          git commit -m "🔄 Mise à jour automatique des vidéos YouTube" || exit 0
          git push
